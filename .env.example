# Environment Configuration Template
# Copy this file to .env and fill in your actual values

# Application Environment
NODE_ENV=development
PORT=9001

# Database Configuration
DB_HOST=localhost
DB_PORT=3308
DB_NAME=xiaoxiao_dushulang
DB_USER=root
DB_PASS=your_database_password

# Security Configuration
SESSION_SECRET=your_super_secure_session_secret_here_at_least_32_characters

# CDN Configuration (Optional)
CDN_BOOTSTRAP=https://cdn.jsdelivr.net/npm/bootstrap@5.1.3
CDN_HANZI=https://cdn.jsdelivr.net/npm/hanzi-writer@3.5.0
CDN_TINYMCE=https://cdn.tiny.cloud/1/your-api-key

# Logging Configuration
LOG_LEVEL=info
LOG_MAX_SIZE=20m
LOG_MAX_FILES=14d

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000
AUTH_RATE_LIMIT_MAX=5

# Security Headers
HELMET_CSP_REPORT_URI=
CORS_ORIGIN=http://localhost:9001