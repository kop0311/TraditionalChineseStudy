<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>学写字 - <%= sentence.simp %></title>
    <link href="<%= CDN_BOOTSTRAP %>" rel="stylesheet">
    <link href="/css/main.css" rel="stylesheet">
    <style>
        /* 写字练习特殊样式 */
        .writing-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 2rem;
            margin: 1rem 0;
            border: 2px solid rgba(76, 175, 80, 0.2);
        }
        
        .character-grid {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin: 2rem 0;
        }
        
        .character-card {
            background: white;
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
            border: 2px solid rgba(76, 175, 80, 0.2);
            text-align: center;
            min-width: 300px;
            transition: transform 0.3s ease;
        }
        
        .character-card:hover {
            transform: translateY(-5px);
        }
        
        .character-display {
            font-size: 4rem;
            color: #2E7D32;
            font-weight: bold;
            margin-bottom: 1rem;
        }
        
        .character-info {
            margin-bottom: 1rem;
        }
        
        .character-pinyin {
            font-size: 1.5rem;
            color: #FF6F00;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        
        .character-meaning {
            color: #666;
            font-size: 1rem;
        }
        
        .hanzi-writer-container {
            margin: 1rem 0;
            display: flex;
            justify-content: center;
        }
        
        .writing-controls {
            margin-top: 1rem;
        }
        
        .stroke-info {
            background: rgba(255, 152, 0, 0.1);
            border-radius: 10px;
            padding: 1rem;
            margin: 1rem 0;
            border: 1px solid rgba(255, 152, 0, 0.3);
        }
        
        .sentence-display {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 1.5rem;
            margin: 2rem 0;
            text-align: center;
            border: 2px solid rgba(76, 175, 80, 0.2);
        }
        
        .sentence-chinese {
            font-size: 2.5rem;
            color: #2E7D32;
            margin-bottom: 1rem;
            font-weight: 600;
        }
        
        .practice-modes {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            padding: 1.5rem;
            margin: 1rem 0;
            border: 2px solid rgba(33, 150, 243, 0.2);
        }
        
        .mode-buttons {
            display: flex;
            justify-content: center;
            gap: 10px;
            flex-wrap: wrap;
            margin-top: 1rem;
        }
        
        .progress-indicator {
            background: rgba(76, 175, 80, 0.1);
            border-radius: 10px;
            padding: 1rem;
            margin: 1rem 0;
            text-align: center;
        }
        
        .progress-bar {
            background: #e9ecef;
            border-radius: 10px;
            height: 20px;
            overflow: hidden;
            margin: 10px 0;
        }
        
        .progress-fill {
            background: linear-gradient(135deg, #4CAF50 0%, #81C784 100%);
            height: 100%;
            transition: width 0.3s ease;
        }
        
        @media (max-width: 768px) {
            .character-card {
                min-width: 250px;
                padding: 15px;
            }
            
            .character-display {
                font-size: 3rem;
            }
            
            .sentence-chinese {
                font-size: 2rem;
            }
            
            .character-grid {
                gap: 15px;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">小小读书郎</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/reader/<%= classic.slug %>/<%= chapter.chapter_no %>">返回阅读</a>
                <a class="nav-link" href="/classics">所有经典</a>
            </div>
        </div>
    </nav>

    <div class="container my-4">
        <!-- 页面标题 -->
        <div class="row mb-4">
            <div class="col-12 text-center">
                <h1 class="mb-3">✏️ 学写字</h1>
                <h2 class="h4 text-muted"><%= classic.title %> · <%= chapter.title %></h2>
            </div>
        </div>

        <!-- 句子显示 -->
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="sentence-display">
                    <div class="sentence-chinese"><%= sentence.simp %></div>
                    <div class="text-muted">点击下面的汉字开始练习写字</div>
                </div>
            </div>
        </div>

        <!-- 练习模式选择 -->
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="practice-modes">
                    <h5 class="text-center mb-3">练习模式</h5>
                    <div class="mode-buttons">
                        <button class="btn btn-primary btn-sm" data-mode="stroke">
                            ✏️ 笔顺练习
                        </button>
                        <button class="btn btn-secondary btn-sm" data-mode="trace">
                            ✏️ 描红练习
                        </button>
                        <button class="btn btn-success btn-sm" data-mode="write">
                            ✏️ 自由书写
                        </button>
                        <button class="btn btn-info btn-sm" data-action="show-all">
                            👀 查看全部
                        </button>
                        <button class="btn btn-warning btn-sm" data-action="animate-all">
                            ▶️ 全部演示
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- 进度指示器 -->
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="progress-indicator">
                    <div>练习进度: <span id="progress-text">0/0</span></div>
                    <div class="progress-bar">
                        <div class="progress-fill" id="progress-fill" style="width: 0%"></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- 汉字练习区域 -->
        <div class="row justify-content-center">
            <div class="col-lg-12">
                <div class="character-grid" id="character-grid">
                    <!-- 这里将通过JavaScript动态填充汉字卡片 -->
                </div>
            </div>
        </div>

        <!-- 导航按钮 -->
        <div class="row mt-4">
            <div class="col-12 text-center">
                <div class="btn-group" role="group">
                    <a href="/reader/<%= classic.slug %>/<%= chapter.chapter_no %>" class="btn btn-outline-primary">
                        ← 返回阅读
                    </a>
                    <a href="/practice/pinyin/<%= sentence.id %>" class="btn btn-outline-secondary">
                        🗣️ 学拼音
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="<%= CDN_HANZI %>"></script>
    <script src="/js/writing-practice-hanzi.js"></script>
    <script>
        // 传递数据到JavaScript
        window.sentenceData = {
            sentence: <%- JSON.stringify(sentence) %>,
            characters: <%- JSON.stringify(characters) %>
        };
    </script>
</body>
</html>