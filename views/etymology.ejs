<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .character-selector {
            max-height: 200px;
            overflow-y: auto;
        }
        .character-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(50px, 1fr));
            gap: 10px;
            padding: 15px;
        }
        .character-btn {
            aspect-ratio: 1;
            font-size: 1.2rem;
            border: 1px solid #dee2e6;
            background: white;
            cursor: pointer;
            transition: all 0.2s;
        }
        .character-btn:hover {
            background: #f8f9fa;
            border-color: #0d6efd;
            transform: scale(1.05);
        }
        .etymology-timeline .card {
            transition: transform 0.2s;
            border: 2px solid transparent;
        }
        .etymology-timeline .card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .etymology-image {
            object-fit: contain;
            border: 1px solid #e9ecef;
            border-radius: 4px;
            padding: 5px;
            background: #f8f9fa;
        }
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 60px 0;
        }
        .search-container {
            max-width: 600px;
            margin: 0 auto;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">📚 小小读书郎</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">首页</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/etymology">字源探索</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/dashboard">👤 <%= user.name %></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/logout">退出</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/login">登录</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/register">注册</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <div class="hero-section">
        <div class="container text-center">
            <h1 class="display-4 mb-4">🔍 汉字字源探索</h1>
            <p class="lead mb-4">探索汉字从甲骨文到金文到小篆的演变历程</p>
            
            <!-- Character Search -->
            <div class="search-container">
                <div class="input-group input-group-lg">
                    <input type="text" class="form-control" id="characterInput" 
                           placeholder="输入一个汉字..." maxlength="1"
                           value="<%= character || '' %>">
                    <button class="btn btn-success" type="button" id="searchBtn">
                        🔍 探索字源
                    </button>
                </div>
                <small class="text-light mt-2 d-block">或者从下方字符列表中选择</small>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="container mt-4">
        <% if (character && etymologyData) { %>
            <!-- Etymology Display -->
            <div class="row">
                <div class="col-12">
                    <div class="etymology-timeline">
                        <div class="text-center mb-4">
                            <h2 class="display-3 text-primary"><%= character %></h2>
                            <h4 class="text-muted">字的演变历程</h4>
                        </div>
                        
                        <div class="row g-4">
                            <div class="col-lg-4 col-md-6">
                                <div class="card h-100 shadow-sm">
                                    <div class="card-header bg-primary text-white text-center">
                                        <h5 class="mb-0">🦴 甲骨文</h5>
                                        <small><%= etymologyData.evolution.oracle_bone.period %></small>
                                    </div>
                                    <div class="card-body text-center">
                                        <% if (etymologyData.evolution.oracle_bone.image_url) { %>
                                            <img src="<%= etymologyData.evolution.oracle_bone.image_url %>" 
                                                 alt="甲骨文" class="etymology-image mb-3" 
                                                 style="max-width: 120px; max-height: 120px;">
                                        <% } else { %>
                                            <div class="text-muted mb-3" style="height: 120px; display: flex; align-items: center; justify-content: center; border: 2px dashed #dee2e6; border-radius: 8px;">
                                                <div>
                                                    <i class="h1">📜</i><br>
                                                    <small>暂无图片</small>
                                                </div>
                                            </div>
                                        <% } %>
                                        <p class="small text-muted">
                                            <%= etymologyData.evolution.oracle_bone.description || '暂无描述' %>
                                        </p>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="col-lg-4 col-md-6">
                                <div class="card h-100 shadow-sm">
                                    <div class="card-header bg-warning text-dark text-center">
                                        <h5 class="mb-0">🏺 金文</h5>
                                        <small><%= etymologyData.evolution.bronze.period %></small>
                                    </div>
                                    <div class="card-body text-center">
                                        <% if (etymologyData.evolution.bronze.image_url) { %>
                                            <img src="<%= etymologyData.evolution.bronze.image_url %>" 
                                                 alt="金文" class="etymology-image mb-3" 
                                                 style="max-width: 120px; max-height: 120px;">
                                        <% } else { %>
                                            <div class="text-muted mb-3" style="height: 120px; display: flex; align-items: center; justify-content: center; border: 2px dashed #dee2e6; border-radius: 8px;">
                                                <div>
                                                    <i class="h1">🏺</i><br>
                                                    <small>暂无图片</small>
                                                </div>
                                            </div>
                                        <% } %>
                                        <p class="small text-muted">
                                            <%= etymologyData.evolution.bronze.description || '暂无描述' %>
                                        </p>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="col-lg-4 col-md-6">
                                <div class="card h-100 shadow-sm">
                                    <div class="card-header bg-success text-white text-center">
                                        <h5 class="mb-0">📏 小篆</h5>
                                        <small><%= etymologyData.evolution.seal.period %></small>
                                    </div>
                                    <div class="card-body text-center">
                                        <% if (etymologyData.evolution.seal.image_url) { %>
                                            <img src="<%= etymologyData.evolution.seal.image_url %>" 
                                                 alt="小篆" class="etymology-image mb-3" 
                                                 style="max-width: 120px; max-height: 120px;">
                                        <% } else { %>
                                            <div class="text-muted mb-3" style="height: 120px; display: flex; align-items: center; justify-content: center; border: 2px dashed #dee2e6; border-radius: 8px;">
                                                <div>
                                                    <i class="h1">📏</i><br>
                                                    <small>暂无图片</small>
                                                </div>
                                            </div>
                                        <% } %>
                                        <p class="small text-muted">
                                            <%= etymologyData.evolution.seal.description || '暂无描述' %>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="text-center mt-4">
                            <div class="card bg-light">
                                <div class="card-body">
                                    <small class="text-muted">
                                        📊 数据统计: 成功获取 <strong><%= etymologyData.success_count %>/3</strong> 个时期的数据 |
                                        🕐 更新时间: <%= new Date(etymologyData.fetched_at).toLocaleString('zh-CN') %>
                                    </small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <hr class="my-5">
        <% } else if (character) { %>
            <!-- Character not found -->
            <div class="row">
                <div class="col-12">
                    <div class="alert alert-warning text-center">
                        <h4>字符 "<%= character %>" 未找到</h4>
                        <p>该字符可能不在我们的数据库中，请尝试其他字符。</p>
                    </div>
                </div>
            </div>
        <% } %>

        <!-- Character Selector -->
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">📚 选择字符</h5>
                        <small class="text-muted">点击任意字符查看其字源演变</small>
                    </div>
                    <div class="card-body character-selector">
                        <div class="character-grid">
                            <% allCharacters.forEach(char => { %>
                                <button class="character-btn btn <%= character === char.simp ? 'btn-primary' : 'btn-outline-secondary' %>" 
                                        onclick="window.location.href='/etymology/<%= char.simp %>'">
                                    <%= char.simp %>
                                </button>
                            <% }) %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-light mt-5 py-4">
        <div class="container text-center">
            <p class="text-muted mb-0">
                © 2024 小小读书郎 | 汉字字源数据通过API获取 | 
                <a href="/" class="text-decoration-none">返回首页</a>
            </p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Character search functionality
        document.getElementById('searchBtn').addEventListener('click', function() {
            const character = document.getElementById('characterInput').value.trim();
            if (character) {
                window.location.href = `/etymology/${encodeURIComponent(character)}`;
            }
        });

        // Enter key search
        document.getElementById('characterInput').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                document.getElementById('searchBtn').click();
            }
        });

        // Only allow single Chinese character input
        document.getElementById('characterInput').addEventListener('input', function(e) {
            const value = e.target.value;
            if (value.length > 1) {
                e.target.value = value.charAt(value.length - 1);
            }
            // Only allow Chinese characters
            if (value && !value.match(/[一-龯]/)) {
                e.target.value = '';
            }
        });
    </script>
</body>
</html>