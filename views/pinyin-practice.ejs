<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>学拼音 - <%= sentence.simp %></title>
    <link href="<%= CDN_BOOTSTRAP %>" rel="stylesheet">
    <link href="/css/main.css" rel="stylesheet">
    <style>
        /* 拼音练习特殊样式 */
        .pinyin-display {
            font-size: 3rem;
            line-height: 1.8;
            text-align: center;
            margin: 2rem 0;
            font-family: 'Consolas', 'Monaco', monospace;
        }
        
        .syllable-container {
            display: inline-block;
            margin: 10px 15px;
            text-align: center;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
            border: 2px solid rgba(76, 175, 80, 0.2);
            transition: transform 0.3s ease;
        }
        
        .syllable-container:hover {
            transform: scale(1.05);
        }
        
        .chinese-char {
            font-size: 4rem;
            font-weight: bold;
            color: #2E7D32;
            display: block;
            margin-bottom: 10px;
        }
        
        .pinyin-breakdown {
            margin-top: 10px;
        }
        
        /* 声母 - 蓝色 */
        .initial {
            color: #1976D2;
            font-weight: bold;
            font-size: 2.5rem;
        }
        
        /* 韵母 - 绿色 */
        .final {
            color: #388E3C;
            font-weight: bold;
            font-size: 2.5rem;
        }
        
        /* 声调 - 橙色 */
        .tone {
            color: #F57C00;
            font-weight: bold;
            font-size: 2rem;
            margin-left: 5px;
        }
        
        .component-label {
            font-size: 1rem;
            color: #666;
            margin-top: 5px;
            font-weight: normal;
        }
        
        .sentence-display {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 1.5rem;
            margin: 2rem 0;
            text-align: center;
            border: 2px solid rgba(76, 175, 80, 0.2);
        }
        
        .sentence-chinese {
            font-size: 2.5rem;
            color: #2E7D32;
            margin-bottom: 1rem;
            font-weight: 600;
        }
        
        .practice-controls {
            text-align: center;
            margin: 2rem 0;
        }
        
        .speed-control {
            margin: 1rem 0;
        }
        
        .practice-mode-selector {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            padding: 1.5rem;
            margin: 1rem 0;
            border: 2px solid rgba(33, 150, 243, 0.2);
        }
        
        .mode-buttons {
            display: flex;
            justify-content: center;
            gap: 10px;
            flex-wrap: wrap;
        }
        
        .pronunciation-guide {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            padding: 1.5rem;
            margin: 1rem 0;
            border: 2px solid rgba(255, 152, 0, 0.2);
        }
        
        @media (max-width: 768px) {
            .pinyin-display {
                font-size: 2rem;
            }
            
            .chinese-char {
                font-size: 3rem;
            }
            
            .initial, .final {
                font-size: 2rem;
            }
            
            .tone {
                font-size: 1.5rem;
            }
            
            .sentence-chinese {
                font-size: 2rem;
            }
            
            .syllable-container {
                margin: 5px 8px;
                padding: 15px;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">小小读书郎</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/reader/<%= classic.slug %>/<%= chapter.chapter_no %>">返回阅读</a>
                <a class="nav-link" href="/classics">所有经典</a>
            </div>
        </div>
    </nav>

    <div class="container my-4">
        <!-- 页面标题 -->
        <div class="row mb-4">
            <div class="col-12 text-center">
                <h1 class="mb-3">🗣️ 学拼音</h1>
                <h2 class="h4 text-muted"><%= classic.title %> · <%= chapter.title %></h2>
            </div>
        </div>

        <!-- 句子显示 -->
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="sentence-display">
                    <div class="sentence-chinese"><%= sentence.simp %></div>
                    <div class="text-muted">点击下面的字符学习发音</div>
                </div>
            </div>
        </div>

        <!-- 拼音分解显示 -->
        <div class="row justify-content-center">
            <div class="col-lg-12">
                <div class="pinyin-display" id="pinyin-display">
                    <!-- 这里将通过JavaScript动态填充 -->
                </div>
            </div>
        </div>

        <!-- 练习模式选择 -->
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="practice-mode-selector">
                    <h5 class="text-center mb-3">练习模式</h5>
                    <div class="mode-buttons">
                        <button class="btn btn-primary btn-sm" onclick="showMode('breakdown')">
                            🔤 拼音分解
                        </button>
                        <button class="btn btn-secondary btn-sm" onclick="showMode('tone')">
                            🎵 声调练习
                        </button>
                        <button class="btn btn-success btn-sm" onclick="showMode('speed')">
                            ⚡ 快速练习
                        </button>
                        <button class="btn btn-info btn-sm" onclick="playAll()">
                            🔊 全部朗读
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- 发音指导 -->
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="pronunciation-guide">
                    <h5 class="text-center mb-3">发音指导</h5>
                    <div class="row text-center">
                        <div class="col-md-4">
                            <div class="initial" style="font-size: 1.5rem;">声母</div>
                            <div class="component-label">辅音开头</div>
                        </div>
                        <div class="col-md-4">
                            <div class="final" style="font-size: 1.5rem;">韵母</div>
                            <div class="component-label">元音部分</div>
                        </div>
                        <div class="col-md-4">
                            <div class="tone" style="font-size: 1.5rem;">声调</div>
                            <div class="component-label">音调变化</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- 导航按钮 -->
        <div class="row mt-4">
            <div class="col-12 text-center">
                <div class="btn-group" role="group">
                    <a href="/reader/<%= classic.slug %>/<%= chapter.chapter_no %>" class="btn btn-outline-primary">
                        ← 返回阅读
                    </a>
                    <a href="/practice/writing/<%= sentence.id %>" class="btn btn-outline-secondary">
                        ✏️ 学写字
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/pinyin-practice.js"></script>
    <script>
        // 传递数据到JavaScript
        window.sentenceData = {
            sentence: <%- JSON.stringify(sentence) %>,
            characters: <%- JSON.stringify(characters) %>
        };
    </script>
</body>
</html>