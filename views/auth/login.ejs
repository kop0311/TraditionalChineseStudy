<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/main.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">小小读书郎</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/auth/register">没有账户？注册</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-4">
                <div class="card mt-5">
                    <div class="card-header text-center bg-success text-white">
                        <h3 class="mb-0">🏠 家长登录</h3>
                        <small>查看孩子的学习进度</small>
                    </div>
                    <div class="card-body p-4">
                        <% if (locals.error) { %>
                            <div class="alert alert-danger">
                                <% if (error === 'missing_fields') { %>
                                    请填写邮箱和密码
                                <% } else if (error === 'invalid_credentials') { %>
                                    邮箱或密码错误
                                <% } else { %>
                                    登录失败，请重试
                                <% } %>
                            </div>
                        <% } %>
                        
                        <form method="POST" action="/auth/login">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <div class="mb-3">
                                <label for="email" class="form-label">邮箱地址</label>
                                <input type="email" class="form-control" id="email" name="email" required>
                            </div>
                            
                            <div class="mb-4">
                                <label for="password" class="form-label">密码</label>
                                <input type="password" class="form-control" id="password" name="password" required>
                            </div>
                            
                            <div class="d-grid">
                                <button type="submit" class="btn btn-success btn-lg">
                                    🔑 登录
                                </button>
                            </div>
                        </form>
                        
                        <div class="text-center mt-4">
                            <a href="#" class="text-muted text-decoration-none">忘记密码？</a>
                        </div>
                    </div>
                </div>
                
                <div class="text-center mt-3">
                    <a href="/" class="text-muted">返回首页</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>